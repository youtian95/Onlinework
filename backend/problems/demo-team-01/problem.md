# 团队作业 Demo 01

- 该题在 `meta.teamwork` 中声明了分组参数。
- 当前示例默认 `team_count=3`，`team_size=3`。
- 子题数量由 `problem.md` 中的 `&lt;subproblem&gt;...&lt;/subproblem&gt;` 区域块数量决定，并应与队伍人数一致。

<subproblem>

## 子题 1

请计算：  $ {{ x }} + {{ y }} = $ {{ input("alpha_answer") }}

</subproblem>

<subproblem>

## 子题 2

请计算：  $ {{ m }} \times {{ n }} = $ {{ input("beta_answer") }}

</subproblem>

<subproblem>

## 子题 3

请计算：  $ {{ p }} - {{ q }} = $ {{ input("gamma_answer") }}

</subproblem>
